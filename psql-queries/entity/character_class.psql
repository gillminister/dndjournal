DROP TABLE IF EXISTS character_class CASCADE;

CREATE TABLE character_class (
  character   INTEGER REFERENCES character(id),
  class       character_class_name,
  level       SMALLINT,
  PRIMARY KEY(character, class)
);
